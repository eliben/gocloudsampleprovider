This is a simple custom provider for the [Go
CDK](https://github.com/google/go-cloud) `blob` API.

The provider is named `gocloudsampleprovider` and it's a clone of `fileblob`;
the URL is registered at `gcspfile://`.

